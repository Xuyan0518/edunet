<view class="container">
  <view class="hero">
    <view class="title">每周汇报</view>
    <view class="subtitle">选择学生查看或新增汇报</view>
    <view class="row">
      <button wx:if="{{isTeacher}}" class="btn-primary" size="mini" bindtap="createEntry">新增汇报</button>
    </view>
  </view>

  <view wx:if="{{!selectedStudentId}}" class="section card student-filter-card">
    <input class="input" placeholder="搜索学生..." value="{{query}}" bindinput="onSearchStudent" />
    <view class="filter-bar">
      <view class="row-between">
        <view class="filter-label">年级筛选</view>
        <picker mode="selector" range="{{gradeOptions}}" value="{{gradeIndex}}" bindchange="onGradeChange">
          <view class="chip">{{gradeOptions[gradeIndex]}}</view>
        </picker>
      </view>
    </view>
  </view>

  <view wx:if="{{!selectedStudentId}}" class="section card">
    <block wx:if="{{loadingStudents}}">
      <view class="list-item">加载中...</view>
    </block>
    <block wx:elif="{{filteredStudents.length === 0}}">
      <view class="list-item muted">暂无学生</view>
    </block>
    <block wx:else>
      <block wx:for="{{filteredStudents}}" wx:key="id">
        <view class="list-item row-between" bindtap="openStudentFeedback" data-id="{{item.id}}">
          <view>
            <view>{{item.name}}</view>
            <view class="muted">年级 {{item.grade || "未填写"}}</view>
          </view>
          <view class="chip">查看</view>
        </view>
      </block>
    </block>
  </view>

  <view wx:if="{{selectedStudentId}}" class="section card">
    <view class="label">选择学生</view>
    <picker mode="selector" range="{{studentNames}}" bindchange="onStudentChange" disabled="{{lockStudent}}">
      <view class="picker">{{selectedStudentName || "请选择学生"}}</view>
    </picker>
  </view>

  <view wx:if="{{selectedStudentId}}" class="section card">
    <view class="section-title">筛选</view>
    <view class="filter-row">
      <view class="filter-chip {{filterMode === 'all' ? 'chip chip-active' : 'chip'}}" data-mode="all" bindtap="setFilterMode">全部</view>
      <view class="filter-chip {{filterMode === 'month' ? 'chip chip-active' : 'chip'}}" data-mode="month" bindtap="setFilterMode">按月</view>
    </view>
    <view wx:if="{{filterMode === 'month'}}" class="filter-detail">
      <picker mode="date" fields="month" value="{{filterMonth}}" bindchange="onFilterMonthChange">
        <view class="chip">{{filterMonth}}</view>
      </picker>
    </view>
  </view>

  <view wx:if="{{selectedStudentId}}" class="section card">
    <view class="section-title">汇报列表</view>
    <block wx:if="{{filteredEntries.length === 0}}">
      <view class="list-item muted">暂无记录</view>
    </block>
    <block wx:for="{{pagedEntries}}" wx:key="id">
      <view class="list-item row-between" bindtap="openDetail" data-week="{{item.weekStarting}}">
        <view>{{item.weekStarting}} - {{item.weekEnding}}</view>
        <view class="chip">查看</view>
      </view>
    </block>
    <view wx:if="{{filteredEntries.length > pageSize}}" class="pagination">
      <button class="btn-outline" size="mini" bindtap="prevPage">上一页</button>
      <view class="muted">第 {{currentPage}} / {{totalPages}} 页</view>
      <button class="btn-outline" size="mini" bindtap="nextPage">下一页</button>
    </view>
  </view>
</view>
