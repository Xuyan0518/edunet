# Retired: this workflow previously published the whole repository directory.
# Keep this file as a migration marker so existing GitHub Actions history remains
# understandable. Use deploy-pages.yml for manual previews and Render for the
# production web/API service.
name: Retired Static Pages Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; use the Manual GitHub Pages Preview workflow."
